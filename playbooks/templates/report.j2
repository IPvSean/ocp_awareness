There are {{ pod_list.resources | length }} pods


Pod Name                    Launch_Date           UID                                      PodIP            HostIP
{% for pod in pod_list.resources %}
{{ pod.metadata.name }}   {% if pod.status.containerStatuses[0].state.running is defined %}{{ pod.status.containerStatuses[0].state.running.startedAt }}{% endif %}  {{ pod.metadata.ownerReferences[0].uid }}     {{ pod.status.podIP }}      {{ pod.status.hostIP }}

{% endfor %}
